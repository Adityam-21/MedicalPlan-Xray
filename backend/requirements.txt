# Runtime dependencies only — what the API needs to serve predictions.
# Training and test tooling lives in requirements-dev.txt.

fastapi==0.136.3
uvicorn==0.48.0
pydantic==2.13.4
python-dotenv==1.2.2
python-multipart==0.0.29

numpy==2.4.6
pandas==3.0.3
# Pinned: plan_model.joblib was fitted with this version.
scikit-learn==1.8.0
scipy==1.18.1
joblib==1.5.3

supabase==2.23.1
